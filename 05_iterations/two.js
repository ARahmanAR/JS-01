let index = 0;
while (index < 10) {
    // console.log(`Valoue of Index is: ${index}`);
    index+=2;
}

let MyArray = [ 'Apple', 'Banana', 'Orange', 'Grapes' ];
let arr = 0;
while (arr < MyArray.length) {
    // console.log(`Fruit at index ${arr} is: ${MyArray[arr]}`);
    arr++;
}

let score = 1;

do {
    console.log(`Score is ${score}`);
    score += 2;
} while (score < 10);